# 学习路线：从零吃透 GeoAI 平台（前端岗 · 4 周冲刺版）

> **目标**：面试官对本项目提问时，能回答上来并有效展开讲解。
> **你的起点**：HTML / CSS / 基础 JS / React / Vue 会用；Python 会一点基础语法。
> **求职方向**：前端岗为主（前端层学到能动手改，其余层学到"讲清原理、扛住追问"）。
> **节奏**：每天 5 小时，4 周（≈140 小时），边学边练。

---

## 如何使用这份文档

**深度分级**（每个知识点都标了目标深度）：

| 级别               | 含义                                                         | 面试场景                           |
| ------------------ | ------------------------------------------------------------ | ---------------------------------- |
| **L1 概念层**      | 一句话讲清"这是什么、为什么需要它"                           | 面试官顺口问术语                   |
| **L2 实现层**      | 知道本项目代码里具体怎么做的（哪个文件、哪个 API、什么流程） | "你们具体怎么实现的？"             |
| **L3 原理/批判层** | 能讲原理、讲对比方案、讲优缺点、指出 bug 和改进方向          | 追问"为什么这么设计？有什么问题？" |

**前端岗的目标深度**：第 1 章全部 **L3**；第 2、3 章 **L2 为主**（3.2 Function Calling、3.3 ReAct、3.5 RAG 争取 L3）；第 4 章 **L2**（4.1 坐标系必须 L3）；第 5、6 章 **L2**。

**⭐ 标记** = 面试高频/必背内容，优先保证。

**配套材料**：每章最后有"面试自测"，第 7 章有完整问题库，第 8 章是每日计划，附录有术语表和资源。

---

# 第 0 章 项目全景：面试开场 5 分钟怎么讲

面试第一题永远是"介绍一下你的项目"。本章给你一个可背诵的叙事框架 + 一条贯穿全项目的主线剧情。

## 0.1 一句话 / 一段话概括（背诵）

- **一句话**：一个基于 LLM Agent 的城市空间智能分析平台——用户用自然语言问空间问题（如"哪里适合开咖啡店"），AI 自动理解意图、规划分析流程、调用 GIS 工具计算、在地图上可视化结果并生成分析报告。
- **一段话（定位 + 价值）**：传统 GIS 系统需要专业人员手动配置缓冲、叠加、权重等模型，门槛高。本项目在 GIS 引擎和用户之间加了一个 **Spatial Agent 智能决策层**：LLM 负责"理解和规划"，GIS 工具负责"计算和验证"，让普通决策者通过自然语言完成空间分析。核心价值是**把 GIS 能力从"工具化"变成"服务化"**——这是讲清项目价值的万能句式。

## 0.2 总体架构（背下来，面试会画）

```
用户交互层   React 19 + TypeScript + Mapbox GL(2D) + Cesium(3D)      :5173
    │  HTTP / SSE
Spatial Agent层  FastAPI + LangGraph ReAct + 意图解析 + RAG报告      :8001
    │  httpx POST（工具调用）
空间能力服务层  FastAPI + Shapely + GeoPandas + SciPy（11 个工具）   :8002
    │  psycopg2 SQL
空间数据层   PostgreSQL 16 + PostGIS 3.4（5 张表 + GiST 索引）        :5432
数据采集层   OSM（Overpass/OSMnx）→ SQL 入库脚本
部署       Docker Compose（4 个服务 + 健康检查 + 数据卷）
```

记忆口诀：**"前端展示、Agent 决策、空间计算、PostGIS 存数据、OSM 采数据、Docker 管部署"**。

## 0.3 ⭐ 主线剧情：一次请求的完整旅程（最重要的一节）

这是整个项目唯一的"主线"，面试 90% 的问题都是这条链路上的某一段。按顺序能背出来，就赢了一半。

> 用户问："分析国贸周边 500 米内适合开咖啡店的位置"

**第 1 幕：前端组装请求**（[store/index.ts:74-157](frontend/src/store/index.ts#L74-L157)）

1. 输入框提交 → `submitQuery(text)`（[ChatPanel.tsx:78-85](frontend/src/components/chat/ChatPanel.tsx#L78-L85)）
2. 先 `abort()` 掉上一个流（[store/index.ts:76](frontend/src/store/index.ts#L76)）
3. **把地图上下文一起发给后端**：`context = { map_bounds: 当前视口四至, selected_geometry: 用户框选几何 }`（[store/index.ts:86-90](frontend/src/store/index.ts#L86-L90)）——这是本项目"地图交互 + 自然语言"双模态输入的关键设计
4. 调用 `agentApi.queryStream()` 并注册 onEvent / onError / onDone 三个回调

**第 2 幕：前端手写 SSE 流式接收**（[agent.ts:21-72](frontend/src/services/agent.ts#L21-L72)）5. 用 **fetch**（不是 EventSource，因为要 POST）发 `/api/agent/query/stream`，绑定 `AbortController.signal` 6. `response.body.getReader()` 逐块读 → `TextDecoder(stream:true)` 解码 → 按 `\n` 切行 → **半包缓冲**（不完整的尾行留到下一块）→ 解析 `data: {...}` 帧 → 回调 7. Vite dev server 代理转发到 8001（[vite.config.ts:14-23](frontend/vite.config.ts#L14-L23)）

**第 3 幕：Agent 意图解析**（[routes.py:115-133](agent/src/api/routes.py#L115-L133) → [parser.py:83-104](agent/src/intent/parser.py#L83-L104)）8. 后端 event_stream 是 async generator，先调 `IntentParser.parse(query)` 9. 用 `ChatOpenAI`（OpenAI 兼容协议，base_url 指向 DeepSeek，temperature=0）发 SystemMessage + HumanMessage，要求输出严格 JSON：`{task_type, location, industry, criteria, geometry_needed}` 10. 因为 **DeepSeek 不支持 response_format**，代码用"JSON prompt + 三级容错提取"（直接解析 → 剥 ```json 代码块 → 正则抠花括号，[parser.py:55-81](agent/src/intent/parser.py#L55-L81)）
11. 推一条 SSE：`{"type":"phase","phase":"intent","status":"done"}`

**第 4 幕：ReAct 循环 —— 规划 + 工具调用**（[planner.py:128-221](agent/src/planning/planner.py#L128-L221)）12. 把 intent JSON + 地图 context JSON 拼成 user message，交给 LangGraph 预构建的 ReAct Agent（`create_react_agent(llm, tools)`，[planner.py:111-117](agent/src/planning/planner.py#L111-L117)）13. `agent.astream(..., stream_mode="updates")`：**每个图节点完成就推送一次增量**——这就是"边执行边推送心流"的机制（[planner.py:171](agent/src/planning/planner.py#L171)）14. 循环过程：LLM 思考（AIMessage 文本 → 前端显示"推理"步骤）→ LLM 输出 tool_call（→ 前端显示"调用 buffer_analysis"）→ ToolNode 执行 handler → httpx POST 调 spatial 服务（[spatial_tools.py:109-124](agent/src/tools/spatial_tools.py#L109-L124)）→ 结果变成 ToolMessage 塞回 messages → LLM 继续决策 → 直到输出不带 tool_call 的最终回答 15. 每个 step 都推 SSE：`{"type":"step","data":{...},"elapsed_s":...,"step_elapsed_s":...}`（[routes.py:163-173](agent/src/api/routes.py#L163-L173)）

**第 5 幕：空间服务执行 GIS 计算**（例：buffer）16. `POST /api/spatial/buffer`（[spatial_routes.py:35-43](spatial/src/api/spatial_routes.py#L35-L43)）→ Pydantic 校验 `BufferRequest(distance>0)` → `compute_buffer`（[buffer.py:26-52](spatial/src/services/buffer.py#L26-L52)）17. **关键套路**：GeoJSON → Shapely `shape()` → `Transformer` 把 EPSG:4326（经纬度）转 EPSG:3857（米）→ `geometry.buffer(500)` → 转回 4326 → 输出 FeatureCollection（**度不能当米算**，见 4.1）18. 或走数据库：`spatial_query` 用白名单表名 + `%s` 参数化 + `geom && ST_MakeEnvelope(...)` bbox 过滤 + `ST_AsGeoJSON`（[query.py:32-97](spatial/src/services/query.py#L32-L97)）

**第 6 幕：RAG 报告生成**（[interpreter.py:44-117](agent/src/reasoning/interpreter.py#L44-L117)）19. 第三个 LLM 实例（temperature=0.3）：先向 Chroma 向量库检索知识（3 个业务查询 × top3，如"{行业} 选址标准"），拼成"## 参考知识库"段落后连同 intent/steps/results 一起喂给 LLM，生成 Markdown 报告（要求引用知识库来源）20. 结果存 SQLite 历史（[history.py:41-62](agent/src/storage/history.py#L41-L62)），推最后一条 SSE：`{"type":"done", report, results, saved_id, timings}`

**第 7 幕：前端三路消费** 21. store 的 onDone 回调组装 AgentResponse（[store/index.ts:130-139](frontend/src/store/index.ts#L130-L139)）22. **消息面板**：流式心流卡片（阶段进度 + 逐步耗时）→ 意图卡片 → 步骤卡片 → 报告卡片（[MessageList.tsx:110-421](frontend/src/components/chat/MessageList.tsx#L110-L421)），报告用 react-markdown 渲染并正则识别"得分: 85"加徽标（[ReportRenderer.tsx:97-108](frontend/src/components/chat/ReportRenderer.tsx#L97-L108)）23. **2D 地图**：addSource(geojson) + addLayer，用 Mapbox expression 按 score 插值着色（[MapView.tsx:98-170](frontend/src/components/map/MapView.tsx#L98-L170)）24. **3D 地图**：同一份数据，score 映射为 `extrudedHeight` 挤出柱高（[CesiumView.tsx:19-58](frontend/src/components/map/CesiumView.tsx#L19-L58)）

**串讲练习**：每天花 10 分钟对着这个链路自述一遍，第 2 周后要求不看文档 5 分钟讲完。

## 0.4 ⭐ 六大核心能力 vs 代码现状对照（面试千万别吹没实现的）

| 规划能力（README 宣传）       | 代码现状                                                                    | 面试口径                                       |
| ----------------------------- | --------------------------------------------------------------------------- | ---------------------------------------------- |
| 1. 自然语言驱动空间分析 Agent | ✅ 完整实现（intent + planner + 11 tools）                                  | 核心亮点，重点讲                               |
| 2. 空间分析计算引擎           | ✅ 11 个工具全部实现                                                        | 可讲具体算法（Dijkstra/TOPSIS）                |
| 3. 时空变化分析               | ⚠️ 雏形：`temporal.py` 只统计 POI 当前态，注释明确真实历史数据需 Ohsome API | 如实说"当前是统计雏形，历史快照是规划"         |
| 4. 遥感 AI 识别（YOLO/SAM）   | ❌ 无任何代码，仅设计文档规划                                               | **别说自己做了**；被问就说"设计规划中，未落地" |
| 5. 三维城市数字孪生           | ⚠️ 基础版：Cesium entities 挤出柱；无真实 3D Tiles 城市模型                 | "3D 可视化已实现，3D Tiles 全城模型是下一步"   |
| 6. 智能空间报告生成           | ✅ 完整实现（LLM + RAG + Markdown 渲染）                                    | 可讲 RAG 全链路                                |

另有两个"宣传 vs 实际"的口径差：CLAUDE.md 和设计文档提到 **MCP 和 PySAL，但代码里都没有**（工具注册是"MCP 风格"的自研 registry，密度分析用的是 SciPy 的 `gaussian_kde` 而非 PySAL）。面试时主动澄清反而加分："设计文档规划过 MCP 协议，当前实现是自研注册中心，双格式导出设计上对齐了 MCP 思路"。

## 0.5 你需要在面试前想清楚的三个事实

1. **这是 AI Coding 协作项目**——不需要回避，但要换打法：你的价值不是"我一行行敲出来的"，而是"我能逐层讲清实现原理、能定位 bug、能说演进方向"。第 7.3 节的 bug 清单就是你反客为主的武器。
2. **准备一份"我改过什么"清单**：从 `git log` / `git diff` 里找自己实际动过的文件（如 ChatPanel、MessageList、spatial_tools 等），每个改动准备 30 秒的"为什么改、怎么改"讲解。这是诚实且有力的回答。
3. **数据是北京朝阳区为主的样例数据**（5396 条 POI、简化行政区、示意人口格网），不是全城生产数据——被问"数据规模"时按实回答。

---

# 第 1 章 前端层（主战场，全部学到 L3）

前端 16 个源码文件。本章按"知识点 → 项目落点 → 面试怎么讲"组织，代码引用均可用 VS Code 直接点开。

## 1.1 TypeScript（本项目具体用法清单）

目标：不是重学 TS，而是**把项目里出现的每种语法吃透**，每个都能举例。

| 语法                            | 项目落点                                                                                                                                                                                                         | 一句话解释                                                              |                                                   |
| ------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------- | ------------------------------------------------- |
| `interface`（对象结构）         | [types/index.ts:9](frontend/src/types/index.ts#L9) `GeoJSONFeature`、[store/index.ts:20](frontend/src/store/index.ts#L20) `AppState`                                                                             | 描述对象形状                                                            |                                                   |
| `type`（别名/联合）             | [types/index.ts:57](frontend/src/types/index.ts#L57) `MapMode = "2d"                                                                                                                                             | "3d"`                                                                   | 给类型起名或做组合                                |
| **字面量联合类型**（替代 enum） | `MapMode`、`DrawMode`、`LayerId`、`PanelTab`、`View`、`type: "Feature"`                                                                                                                                          | 项目完全不用 enum，全部字面量联合——现代 TS 风格                         |                                                   |
| 泛型                            | `create<AppState>()`（[store/index.ts:50](frontend/src/store/index.ts#L50)）、`agentClient.post<AgentResponse>()`（[agent.ts:13](frontend/src/services/agent.ts#L13)）、`useRef<mapboxgl.Map                     | null>`（[MapView.tsx:36](frontend/src/components/map/MapView.tsx#L36)） | 参数化类型，axios/Zustand 的 API 全靠它做类型提示 |
| 非空断言`!`                     | `getElementById("root")!`（[main.tsx:5](frontend/src/main.tsx#L5)）                                                                                                                                              | 告诉编译器"这里一定有值"                                                |                                                   |
| `as const` 常量断言             | `"Polygon" as const`（[DrawTool.tsx:64](frontend/src/components/map/DrawTool.tsx#L64)）、`(["intent","planning","report"] as const)`（[MessageList.tsx:122](frontend/src/components/chat/MessageList.tsx#L122)） | 把类型收窄到字面量，数组 map 时保留精确类型                             |                                                   |
| 工具类型`Record`                | `Record<string, React.CSSProperties>`（[ChatPanel.tsx:4](frontend/src/components/chat/ChatPanel.tsx#L4)）                                                                                                        | 键值对对象类型                                                          |                                                   |
| 可选链`?.`                      | `f.properties?.score`（[MapView.tsx:107](frontend/src/components/map/MapView.tsx#L107)）                                                                                                                         | 安全访问可能不存在的属性                                                |                                                   |
| 运行时类型守卫                  | `typeof f.properties?.score === "number"`（[MapView.tsx:107](frontend/src/components/map/MapView.tsx#L107)）                                                                                                     | 后端 JSON 不可信，用 typeof 判断后再分分支——前后端接口的核心防御手段    |                                                   |
| `import type`                   | [store/index.ts:2](frontend/src/store/index.ts#L2)                                                                                                                                                               | 纯类型导入，编译后擦除                                                  |                                                   |

💬 **面试怎么问**："为什么项目用 TS？" → ① 前后端协议有明确类型契约（`AgentResponse`/`AgentStep` 与后端 Pydantic 对应，改接口能提前发现）；② 地图库（Mapbox/Cesium）对象结构复杂，无类型寸步难行；③ `strict: true` 全开。追问"enum 为什么不用" → 字面量联合类型更轻量、无运行时对象、可配合 `as const`，是社区推荐替代。

## 1.2 Zustand 状态管理（[store/index.ts](frontend/src/store/index.ts) 全文件精读）

**本项目用到的 API**（全项目只有一个 store，169 行）：

- `create<AppState>((set, get) => ...)` 基础创建（:50）
- **selector 订阅**：每个组件只订阅自己需要的字段（如 [MapView.tsx:40-44](frontend/src/components/map/MapView.tsx#L40-L44) 订阅 5 个字段），字段不变不重渲染——对比 Context 的全局重渲染，这是 Zustand 的核心优势
- 函数式更新 + 展开做不可变：`set((s) => ({ layerVisibility: {...s.layerVisibility, [id]: visible} }))`（:69-72）
- `get()` 在 action 里读即时状态（:76 取消旧请求、:86 组装 context）
- **`AbortController` 存在 store 里**（:35）：跨组件共享"取消句柄"的经典模式，新请求进来先 abort 旧的（:76）
- 未用 middleware（persist/devtools）——能说出"为什么不用 persist：历史记录走服务端 API 而非 localStorage"就是理解深度的体现

💬 **面试怎么问**："为什么用 Zustand 不用 Redux？" → 项目状态量小、心智模型简单（一个 store 装地图域 + Agent 域）；selector 精确订阅避免无关重渲染；样板代码少。"AbortController 为什么放 store？" → 取消动作发生在 store 的 action 里，需要跨组件访问同一实例。

## 1.3 ⭐ 通信层：axios + 手写 SSE 流式解析（[agent.ts:21-72](frontend/src/services/agent.ts#L21-L72)）

这是**前端最有面试价值的一段代码**，必须逐行讲得出。

**为什么不用 EventSource**：SSE 标准的 EventSource 只支持 GET，而本项目要 POST 携带 `{query, context}`。

**实现要点**：

1. `fetch(url, { method:"POST", signal: controller.signal })`（:30-34）——AbortController 实现"取消上一个请求"
2. `response.body.getReader()` 循环 `read()`（:42-50）——ReadableStream 流式读响应体
3. `new TextDecoder().decode(value, { stream: true })`（:52）——`stream:true` 保证 UTF-8 多字节字符被截断在两块之间时不会变成乱码
4. **半包缓冲**：`lines = buffer.split("\n"); buffer = lines.pop() || ""`（:53-54）——网络分包边界和行边界不对齐，最后一个不完整行必须留给下一次循环
5. 只解析 `data: ` 前缀行 → `JSON.parse`（:57-62）
6. 取消判定：`err.name !== "AbortError"` 才当错误（:68）——用户主动取消不算错误

💬 **面试必问**："SSE 和 WebSocket 的区别/为什么选 SSE？" → SSE 是 HTTP 上的单向长连接（服务器→客户端），基于纯 HTTP 实现简单、天然支持断线重连；本项目只需服务器推结果、客户端不需要频繁上行，单向足够；WebSocket 是全双工，适合聊天室/协作编辑。追问"axios 能不能流式？" → axios 基于 XHR，XHR 只能通过 `onDownloadProgress` 拿累计文本，体验和内存控制都不如 fetch ReadableStream。

## 1.4 ⭐ Mapbox GL（[MapView.tsx](frontend/src/components/map/MapView.tsx) + [DrawTool.tsx](frontend/src/components/map/DrawTool.tsx)）

**核心心智模型：source 是数据，layer 是样式。** 一个 source 可以挂多个 layer。

| 知识点                      | 落点                                                                     | 说明                                                                                                                  |
| --------------------------- | ------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------- |
| Map 初始化                  | [MapView.tsx:50-55](frontend/src/components/map/MapView.tsx#L50-L55)     | `container/style/center/zoom`；style 用 `mapbox://styles/mapbox/light-v11`                                            |
| addSource / addLayer        | [MapView.tsx:110-120](frontend/src/components/map/MapView.tsx#L110-L120) | 先加`{type:"geojson", data}` 数据源，再加 fill/line 图层                                                              |
| **expression 数据驱动着色** | [MapView.tsx:125-135](frontend/src/components/map/MapView.tsx#L125-L135) | `["interpolate", ["linear"], ["get","score"], 0,"#f44336", ..., 100,"#003300"]`——按 feature 的 score 属性线性插值颜色 |
| 事件：load / moveend        | [MapView.tsx:59-67](frontend/src/components/map/MapView.tsx#L59-L67)     | moveend（拖动结束）时把`getBounds()` 写入 store 的 `mapBounds`——地图状态"上抛"，随问题发给后端                        |
| 框选绘制                    | [DrawTool.tsx:35-91](frontend/src/components/map/DrawTool.tsx#L35-L91)   | mousedown/mouseup 取 lngLat → min/max 归一化四角 → 0.001 度阈值过滤误点 → 手写闭合 Polygon → 写 store                 |
| fitBounds 聚焦              | [MapView.tsx:158-166](frontend/src/components/map/MapView.tsx#L158-L166) | 遍历结果坐标`LngLatBounds.extend` 后 `fitBounds({padding, maxZoom})`                                                  |
| 生命周期                    | [MapView.tsx:71-74](frontend/src/components/map/MapView.tsx#L71-L74)     | cleanup 里`map.remove()`——地图实例与 React 生命周期的经典配合                                                         |

💬 **面试怎么问**："source 和 layer 什么关系？" / "地图结果怎么按分数上色？"（expression 的 `get` 从 properties 取值）/"地图框选怎么实现的？"（原生鼠标事件 + 手写 GeoJSON + store 中转）/ "为什么在 moveend 而不是 move 同步 bounds？"（move 频率过高，moveend 只在拖拽结束触发一次）。

## 1.5 ⭐ Cesium（[CesiumView.tsx](frontend/src/components/map/CesiumView.tsx)）

| 知识点                  | 落点     | 说明                                                                                                                                                                                               |
| ----------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Ion Token / 全球地形    | :68-73   | `Ion.defaultAccessToken`；`Terrain.fromWorldTerrain()`                                                                                                                                             |
| Viewer 初始化与控件裁剪 | :72-82   | 关掉 animation/timeline 等默认控件，只留场景                                                                                                                                                       |
| 相机飞行                | :84-91   | `camera.flyTo({destination, orientation: {heading, pitch, roll}})`，角度用 `toRadians` 转弧度                                                                                                      |
| **Entity API 挤出柱**   | :29-40   | `polygon: {hierarchy, extrudedHeight: Math.max(score*2, 10)}`——**score 直接映射为柱高**，这就是"数据驱动 3D"；`Cartesian3.fromDegreesArray` 需要 [lon,lat,lon,lat] 扁平数组，所以先 `flatMap` 拍平 |
| GeoJsonDataSource       | :119-126 | 无 score 的结果走`Cesium.GeoJsonDataSource.load(fc, options)`，一条龙加载                                                                                                                          |
| 更新与销毁              | :102-135 | 结果变化时`entities.removeAll()` 清场重渲 → `flyTo` 聚焦；cleanup `viewer.destroy()`                                                                                                               |

💬 **面试怎么问**："2D 和 3D 怎么共存的？" → 同一个 store（`mapMode` 切换条件渲染，[AppLayout.tsx:107](frontend/src/components/layout/AppLayout.tsx#L107)），同一份 `agentResponse`，两套渲染策略：2D 用 expression 颜色、3D 用挤出高度。"entities 和 GeoJsonDataSource 怎么选？" → entities 可精细控制单个要素（挤出、样式、pick），GeoJsonDataSource 批量加载简单。

## 1.6 GeoJSON 规范（贯穿全项目的通用格式）

- 结构：`FeatureCollection { type, features: [ Feature { type:"Feature", properties, geometry } ] }`
- 前端类型定义在 [types/index.ts:4-13](frontend/src/types/index.ts#L4-L13)（coordinates 用三层嵌套联合覆盖点/线/面）
- **Polygon 首尾点必须闭合**——本项目手写框选几何时显式处理了（[DrawTool.tsx:63-72](frontend/src/components/map/DrawTool.tsx#L63-L72)）
- 前后端通用：后端 `ST_AsGeoJSON` / Shapely `__geo_interface__` 产出，前端 source/DataSource 直接消费——这是"GIS 全栈"的通用语言，面试聊协作时必提

## 1.7 React 细节与工程素养

| 知识点                  | 落点                                                                                  | 面试讲法                                                                                                                                    |
| ----------------------- | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| StrictMode 双执行       | [main.tsx:5-9](frontend/src/main.tsx#L5-L9)                                           | 开发环境 effect 会 mount→unmount→mount，地图初始化必须能容忍重复挂载                                                                        |
| useRef 存瞬时 UI 状态   | [AppLayout.tsx:20](frontend/src/components/layout/AppLayout.tsx#L20)                  | 侧栏拖拽用 ref 存`dragging` 标志而非 state——高频变化但不触发渲染，教科书式案例；配合全局 mousemove/mouseup + cleanup 解绑（:29-44）         |
| 内联样式模式            | 全项目无 .css 文件                                                                    | `Record<string, React.CSSProperties>` 集中定义样式对象——本项目工程约定，可对比 Tailwind/CSS Modules 讲取舍                                  |
| react-markdown 组件覆写 | [ReportRenderer.tsx:60-174](frontend/src/components/chat/ReportRenderer.tsx#L60-L174) | 覆写 15 个 markdown 元素；`p` 渲染器正则识别"得分: 85"插 ScoreBadge（:97-108）；`li` 渲染器匹配风险词染红（:146-150）；默认转义 HTML 防 XSS |

## 1.8 工程化（[vite.config.ts](frontend/vite.config.ts)）

- **双后端代理**（:14-23）：`/api/agent`→8001、`/api/spatial`→8002——前端只写相对路径，由 dev server 转发，**从根上规避跨域**
- `import.meta.env.VITE_MAPBOX_TOKEN`：Vite 只把 `VITE_` 前缀的变量暴露给客户端（[MapView.tsx:9-11](frontend/src/components/map/MapView.tsx#L9-L11)）
- `vite-plugin-cesium`：把 Cesium 的 Assets/Workers/Widgets 静态资源从 node_modules 拷进构建产物并注入 `CESIUM_BASE_URL`——不配插件 Cesium 跑不起来
- tsconfig：`strict: true`、`jsx: "react-jsx"`（React 17+ 新转换，无需手动 import React）、`moduleResolution: "bundler"`

💬 **面试怎么问**："跨域怎么解决的？" → 开发用 Vite proxy（浏览器只跟 5173 同源通信）；生产用 Nginx 反代或同域部署；后端 CORS 目前 `allow_origins:["*"]` 是开发态写法，生产必须收紧（这个风险能主动说出来是加分项）。

## 1.9 前端面试自测（学完本章先答这 5 题）

1. 从用户点发送到地图出结果，前端代码经过了哪几个文件和函数？（答不出来 → 回 0.3）
2. 为什么 SSE 解析要半包缓冲？`TextDecoder` 的 `stream:true` 解决什么问题？
3. Mapbox 的 source 和 layer 分别是什么？score 上色用的哪段 expression？
4. Cesium 的挤出柱怎么把 score 变成高度的？
5. Zustand 的 selector 订阅解决了什么问题？AbortController 为什么放 store？

---

# 第 2 章 Python 后端基础（读懂代码所需，L2）

你有基础语法，本章只补"读本项目代码绕不开"的部分，不从头学。

## 2.1 本项目代码里的 Python 语法点

| 语法                  | 落点                                                                                                                                                    | 说明                                                                        |                             |
| --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- | --------------------------- |
| 类型注解              | `list[float]                                                                                                                                            | None`（[query.py:16](spatial/src/services/query.py#L16)）、`dict[str, Any]` | Python 3.10+ 写法，读懂即可 |
| `async def` / `await` | [routes.py:115](agent/src/api/routes.py#L115) async generator、[spatial_tools.py:109-124](agent/src/tools/spatial_tools.py#L109-L124) httpx AsyncClient | 异步函数可挂起等待 IO，不阻塞事件循环                                       |                             |
| generator（yield）    | [routes.py:115-232](agent/src/api/routes.py#L115-L232) `event_stream`                                                                                   | 边产出边返回——SSE 的实现基础                                                |                             |
| 上下文管理器          | [main.py:27-38](spatial/src/main.py#L27-L38) `asynccontextmanager` lifespan                                                                             | 启动/关闭时执行初始化与清理                                                 |                             |
| f-string              | 全项目 SQL 拼接                                                                                                                                         | 字符串插值                                                                  |                             |

## 2.2 FastAPI 核心概念（每个都能一句话讲）

- **路由组织**：`APIRouter` + `include_router(prefix=...)`（[main.py:56-57](spatial/src/main.py#L56-L57)）——按业务拆路由文件
- **`response_model`**：声明响应结构，FastAPI 自动校验并序列化（[spatial_routes.py:35](spatial/src/api/spatial_routes.py#L35)）
- **同步 `def` 端点跑在线程池**：spatial 服务的 11 个端点全是同步 `def`（psycopg2 是同步驱动），FastAPI 自动丢线程池执行，不阻塞事件循环——这是理解"为什么同步代码也能并发服务"的关键
- **`HTTPException(status_code, detail)`**：抛异常返回错误码（[spatial_routes.py:41](spatial/src/api/spatial_routes.py#L41)），本项目约定 ValueError→400、其他→500
- **CORS 中间件**（[main.py:48-54](spatial/src/main.py#L48-L54)）
- **lifespan**：应用生命周期钩子（[main.py:27-38](spatial/src/main.py#L27-L38)）
- 启动方式：`uvicorn src.main:app --reload --port 8002`

## 2.3 Pydantic v2（项目里它是"接口契约 + 工具 schema"双重身份）

- `BaseModel` + `Field(gt=0, ge=10, le=200, min_length=2, description=...)`——声明式校验（[schemas.py](spatial/src/models/schemas.py) 全文 17 个模型）
- `Literal` 枚举约束：`operation: Literal["intersection","union","difference"]`（[schemas.py:98](spatial/src/models/schemas.py#L98)）
- `model_dump()` 序列化
- **重要**：在 Agent 层，Pydantic 的 `Field(description=...)` 直接变成 LLM 可见的工具参数说明（[spatial_tools.py:23-102](agent/src/tools/spatial_tools.py#L23-L102)）——"一处定义，两处使用"，这是 3.4 节的伏笔

## 2.4 动手练习（产出物 = 你的 Python 作品）

1. 用 FastAPI 写一个 `/api/hello` POST 接口：Pydantic 校验 body（name 必填、age 0-120），返回 JSON
2. 给它加一个 SSE 接口：async generator 每秒 yield 一个数字，前端（就用本项目 agent.ts 的解析代码）消费它
3. 读 [buffer.py](spatial/src/services/buffer.py) 全文并注释每行

---

# 第 3 章 Agent / LLM 层（L2 为主，⭐ 处争取 L3）

这层是整个项目的灵魂，也是你最不熟、最容易被问穿的区域。**目标是：术语随口讲、流程能画图、原理扛得住两轮追问。**

## 3.1 LLM 基础概念（L1，但必须非常扎实）

| 概念                          | 一句话                                                                          | 项目落点                                                                                                                                                                                                                                                                    |
| ----------------------------- | ------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| token                         | 模型处理文本的最小单位（中文约 1 字 ≈ 1-2 token）                               | 所有与模型的交互都是 messages 列表                                                                                                                                                                                                                                          |
| messages 结构                 | System（人设规则）/ Human（用户输入）/ AI（模型输出）/ Tool（工具结果）四类消息 | [parser.py:86-90](agent/src/intent/parser.py#L86-L90)、[planner.py:164](agent/src/planning/planner.py#L164)                                                                                                                                                                 |
| 上下文窗口                    | 一次请求能塞进的最大 token 数，超了要截断                                       | 本项目结果直接全量 JSON 化进 prompt，长结果有截断风险（可讲改进点）                                                                                                                                                                                                         |
| **temperature（温度）**       | 采样随机性：0=确定、1=发散                                                      | **本项目三处对比是绝佳素材**：意图解析 0（[parser.py:52](agent/src/intent/parser.py#L52)）、规划 0（[planner.py:107](agent/src/planning/planner.py#L107)）、报告 0.3（[interpreter.py:41](agent/src/reasoning/interpreter.py#L41)）——"解析和决策要确定性，报告允许一点文采" |
| 为什么 LLM 不能直接做空间分析 | LLM 没有坐标计算/拓扑/数据库能力，只能理解和规划                                | 见[第二章设计文档 2.2 节](第二章 Agent 总体架构设计.md)——"LLM 负责理解和规划，GIS 工具负责计算和验证"                                                                                                                                                                       |

## 3.2 ⭐ OpenAI 兼容 API 与 Function Calling（L3）

**OpenAI 兼容协议**：一个事实上的行业标准 HTTP 协议，任何实现它的服务都能被 `ChatOpenAI` 调用。本项目 `.env` 里 `OPENAI_BASE_URL=https://api.deepseek.com/v1`、`LLM_MODEL=deepseek-v4-pro`（[planner.py:103-108](agent/src/planning/planner.py#L103-L108)）——**同一套代码换 key 和 base_url 就能换模型厂商**，这是"兼容协议"的商业价值。

**Function Calling 机制**（Agent 的核心，必须能讲）：

1. 请求时把工具清单以 JSON Schema 形式放进请求（本项目 schema 来自 Pydantic 自动生成，[registry.py:49-56](agent/src/tools/registry.py#L49-L56)）
2. 模型不返回自然语言，而是返回"要调哪个函数 + 什么参数"的结构化 JSON：`{"name":"buffer_analysis","arguments":{"distance":500}}`
3. 执行方按参数调用真实函数，把结果作为 ToolMessage 塞回 messages，让模型继续
4. 前端看到的 `AIMessage.tool_calls` 解析在 [planner.py:36-43](agent/src/planning/planner.py#L36-L43)

**结构化输出的两条路线**（追问深度点）：① 模型原生 JSON mode（OpenAI 的 `response_format`）② prompt 约束 + 手动解析。本项目因为 **DeepSeek 不支持 response_format**，走的是路线②：严格 JSON 模板 prompt + 三级容错提取（直接解析 → 剥代码块 → 正则抠花括号，[parser.py:55-81](agent/src/intent/parser.py#L55-L81)）。**能讲出"为什么不用框架自带能力"就是深度**。

## 3.3 ⭐ ReAct 与 LangGraph（L3）

**ReAct = Reasoning + Acting**：模型交替做"思考"和"调工具"，直到能给出最终答案。本项目用 LangGraph 预构建的 `create_react_agent(llm, tools)`（[planner.py:111-117](agent/src/planning/planner.py#L111-L117)），内部是图：`agent 节点（LLM 决策）⇄ tools 节点（ToolNode 执行）`，条件边为"有 tool_call 就进 tools 节点，没有就结束"，State 只有一个字段 `messages`（[planner.py:164](agent/src/planning/planner.py#L164)）。

**流式心流的实现**（[planner.py:128-221](agent/src/planning/planner.py#L128-L221)，本项目 Agent 层最大亮点）：

- `agent.astream(input, stream_mode="updates")`：**每个图节点完成一次就推送一次增量输出**（:171）
- `stream_mode` 三种对比：`values`（每次推全量状态，最重）/ `updates`（每次推节点增量，本项目）/ `messages`（token 级流式，本项目报告未用，是演进方向）
- 因为 updates 模式不同节点输出可能重复，**按消息 id 去重**（:178-185）
- `_parse_messages` 把增量消息翻译成前端可展示的步骤：`tool_call`（调什么工具+参数）/ `reasoning`（推理文本，截前 500 字）/ `tool_result`（要素数量）（:28-72）
- 每步带 `elapsed_s`（距请求开始）和 `step_elapsed_s`（距上一步）——注意 step 间隔里包含了 LLM 下一次决策的思考时间，是排查慢步骤的利器

**记忆设计对比**（追问点）：LangGraph 的多轮记忆靠 `checkpointer`（按 thread_id 持久化对话状态），本项目**没有启用**——每次请求都是无状态新对话，历史记忆靠自建 SQLite 补偿（[history.py](agent/src/storage/history.py)）。能讲清"框架记忆 vs 业务审计记忆"的职责分离就是深度。

## 3.4 ⭐ 工具注册与业务级封装（[registry.py](agent/src/tools/registry.py) + [spatial_tools.py](agent/src/tools/spatial_tools.py)）

**一份 schema 双格式导出**（设计亮点）：Pydantic 模型 → `model_json_schema()` 给 LLM 看（OpenAI Function 格式，:26-35）；同一个模型 → `StructuredTool` 给执行器用（:37-46）。单一事实来源，改 schema 两处同步。

**GIS Tool 设计原则**（CLAUDE.md 明确约定）：**Agent 不直接调用底层 GIS 函数**（如 `ST_Buffer`），只暴露业务级工具（如 `buffer_analysis`），内部封装 PostGIS/Shapely 调用。为什么：LLM 不理解数据库函数，业务级描述（"计算空间对象指定距离范围"）它才能正确选用。

**业务级封装最佳案例——`_distance_analysis` 降级链路**（[spatial_tools.py:151-183](agent/src/tools/spatial_tools.py#L151-L183)）：

1. 先尝试把输入解析成 GeoJSON
2. 失败 → 调 `_geocode` 地名转坐标：查 POI 表 50 条，先精确匹配再模糊匹配
3. 再失败 → **不报异常，返回带 hint 的结构化错误**："无法从数据库找到坐标……北京常见地标坐标：天安门=[116.397,39.909]……"
4. 这个 hint 作为 ToolMessage 回给 LLM，**驱动 LLM 用自带知识自我纠错重试**——"把错误信息当作 prompt"的经典手法，非常值得在面试里主动讲

11 个工具（名称 → spatial 端点）速记：`buffer_analysis→/buffer`、`distance_analysis→/distance`、`spatial_query→/query`、`route_analysis→/route`、`overlay_analysis→/overlay`、`density_analysis→/density`、`suitability_analysis→/suitability`、`temporal_analysis→/temporal`、`cost_distance_analysis→/cost-distance`、`voronoi_analysis→/voronoi`、`mcda_analysis→/mcda`。统一管道：httpx AsyncClient、60 秒超时、统一日志（[spatial_tools.py:109-124](agent/src/tools/spatial_tools.py#L109-L124)）。

## 3.5 ⭐ RAG 全链路（[vector_store.py](agent/src/knowledge/vector_store.py) + [interpreter.py](agent/src/reasoning/interpreter.py)）

**RAG = Retrieval-Augmented Generation（检索增强生成）**：生成前先从知识库检索相关资料拼进 prompt，让回答"有据可依"。解决：模型知识过时、私有/领域知识缺失、幻觉。

**本项目全链路**：

1. **知识源**：55 条城市规划/选址规范 JSON（[knowledge_items.json](agent/src/knowledge/data/knowledge_items.json)）
2. **向量化**：ChromaDB 内置 ONNX 本地模型（all-MiniLM-L6-v2）做 embedding——**文本转高维向量，无需 GPU 和外部 API**（[vector_store.py:24-25](agent/src/knowledge/vector_store.py#L24-L25)）
3. **入库**：`col.add(documents, ids, metadatas)`，每条知识一个文档（[vector_store.py:30-55](agent/src/knowledge/vector_store.py#L30-L55)）
4. **检索**：3 个业务查询（如"{行业} 选址标准"）各取 top3，`content[:40]` 去重后拼成"## 参考知识库"段落（[interpreter.py:44-73](agent/src/reasoning/interpreter.py#L44-L73)）——注意**查询是业务关键词模板而非原始用户问题**，这叫 query 改写
5. **生成**：要求报告"必须引用知识库来源"（[interpreter.py:15-30](agent/src/reasoning/interpreter.py#L15-L30)）

**可主动讲的 3 个局限**（批判性阅读素材）：① 文档未做切块（chunking），长文档检索粒度粗；② 未设相似度阈值，top_k 内全要，可能引入无关知识；③ 代码把 Chroma 返回的距离（distance，越小越相似）直接命名为 score，语义易误导。

**embedding 一句话原理**：用模型把文本映射到向量空间，语义相近的文本向量距离近——"咖啡店选址标准"和"奶茶店选址"向量距离近，检索就能命中。

## 3.6 Prompt 工程落点（能逐条指出本项目做了什么）

| 手法              | 落点                                                                                                                                                                             |
| ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 人设 + 能力边界   | 意图解析 prompt 明确 6 种任务类型，统计/常识问题必须判`unsupported`（[parser.py:16-41](agent/src/intent/parser.py#L16-L41)）                                                     |
| 工作流程指导      | 系统提示里教 LLM 高效用工具："先 spatial_query 摸底再做针对性分析；选址最多 3-4 步；地名查不到就用模型自身知识估算"（[planner.py:89-95](agent/src/planning/planner.py#L89-L95)） |
| 输出格式约束      | 反引号仅用于代码、强调用粗体、坐标用括号——针对 DeepSeek 输出毛病的"格式工程"（[planner.py:97-100](agent/src/planning/planner.py#L97-L100)）                                      |
| 错误信息即 prompt | 3.4 节的距离分析 hint 设计                                                                                                                                                       |
| JSON 输出模板     | 意图解析给严格 JSON 模板（[parser.py:18-26](agent/src/intent/parser.py#L18-L26)）                                                                                                |

## 3.7 服务端 SSE 与三阶段多 LLM 架构

**SSE 协议**（[routes.py:112-232](agent/src/api/routes.py#L112-L232)）：`data: {json}\n\n` 帧格式 + `StreamingResponse(media_type="text/event-stream")`；4 类事件：`phase`（intent/planning/report + running/done）、`step`（带两种耗时）、`done`（完整结果）、`error`（错误也走 SSE 通道，不破坏连接）。当前是**阶段/步骤级流式**，报告整块到达——"token 级流式（stream_mode="messages"）"是能讲的演进方向。

**三阶段多 LLM 架构**（[routes.py:52-103](agent/src/api/routes.py#L52-L103)）：意图解析、任务规划、报告生成用**三个独立 LLM 实例、三种温度、三种 prompt 策略**。本质是把一个复杂任务拆成多个可控 LLM 子任务——好处：可单独测试、可单独替换模型、成本可控（代码里 `timings` 全程埋点记录各阶段耗时）。对比"单 Agent 一把梭"能讲出一堆深度。

## 3.8 Agent 层面试自测（5 题）

1. Function Calling 的完整流程是什么？模型返回的 tool_call 是谁执行的？
2. ReAct 循环的"退出条件"是什么？
3. `stream_mode="updates"` 和 `values`、`messages` 有什么区别？本项目为什么选它？
4. RAG 在本项目里从知识入库到报告引用来源的全链路是什么？
5. 为什么意图解析、规划、报告要拆成三个 LLM 实例？

---

# 第 4 章 GIS 核心概念与空间服务层（L2，4.1 必须 L3）

这是"项目护城河"部分——前端岗候选者如果 GIS 概念讲得清，差异立现。

## 4.1 ⭐ 坐标系与投影（整个项目最重要的横切概念，L3）

**核心命题：经纬度是"度"，不是"米"，度不能直接当米算。**

- **EPSG:4326 = WGS84**：经纬度坐标系，单位度。北京 1 度经度 ≈ 85 km、1 度纬度 ≈ 111 km，且经度的实际距离随纬度变化。
- **EPSG:3857 = Web Mercator（网络墨卡托）**：米制平面投影，Google 地图/Mapbox 用的就是它。
- **本项目固定套路**（出现 7 处）：`Transformer.from_crs("EPSG:4326", "EPSG:3857", always_xy=True)` → 计算 → 转回 4326 输出。落点：[buffer.py:33-42](spatial/src/services/buffer.py#L33-L42)、[distance.py:35-41](spatial/src/services/distance.py#L35-L41)、[overlay.py:45-52](spatial/src/services/overlay.py#L45-L52) 等。`always_xy` 保证先经度后纬度（顺序坑，常被问）。
- **3857 的失真**：Web Mercator 面积随纬度放大（格陵兰看起来和非洲一样大）。本项目 `buffer.py:49` 的 `area_sqm` 在 3857 下计算，高纬度会偏大——北京纬度失真可接受，但这是"精度意识"考点。
- **ST_SetSRID vs ST_Transform 经典误区**：SetSRID 只是"贴标签"（声明这是什么坐标系，坐标值不变）；Transform 才是真重投影（坐标值改变）。本项目 SQL 里只用 SetSRID/GeomFromText 入库（贴 4326 标签），重投影全在 Python 层做。

💬 **面试必问**："buffer 500 米为什么不直接 buffer(500)？" → 4326 下 buffer(500) 是 500 度，毫无意义；必须转米制投影。追问"为什么用 3857？" → 米制、前端地图同款投影、北京中纬度失真可接受；更精确可选局部 UTM 投影。

## 4.2 Shapely（几何计算引擎）

心智模型：**Python 里的几何对象库**，一切 GIS 计算的底层。本项目落点：

- GeoJSON↔几何互转：`shape()`（dict→几何，[buffer.py:27](spatial/src/services/buffer.py#L27)）、`__geo_interface__` / `mapping()`（几何→dict）
- 空间操作：`buffer()`（:39）、`distance()`、`intersection()`（[suitability.py:81](spatial/src/services/suitability.py#L81)）、`unary_union`（合并多个几何，[suitability.py:49](spatial/src/services/suitability.py#L49)）、`voronoi_diagram`（[voronoi.py:66](spatial/src/services/voronoi.py#L66)）
- 拓扑谓词（判断空间关系）：`intersects`（相交）、`covers`（覆盖，[voronoi.py:86](spatial/src/services/voronoi.py#L86)）
- `shapely.ops.transform(func, geom)`：对几何整体做坐标变换（配合 pyproj Transformer，[buffer.py:36](spatial/src/services/buffer.py#L36)）

## 4.3 GeoPandas（空间数据表格）

心智模型：**Pandas DataFrame + 一个 geometry 列 + 一个 crs 属性**，每行是一个空间要素。本项目只在 overlay 用：`GeoDataFrame.from_features(features, crs="EPSG:4326")` → `.to_crs("EPSG:3857")` → `.overlay(gdf_b, how="intersection"|"union"|"difference")` → 转回 4326（[overlay.py:38-54](spatial/src/services/overlay.py#L38-L54)）。三个 how 对应 GIS 里的 Intersect/Union/Erase 工具。

## 4.4 11 个空间分析工具逐个讲（每个都要能说"干什么 + 怎么做 + 一句原理"）

| 工具                   | 干什么                                   | 实现要点（含落点）                                                                                                                                        |
| ---------------------- | ---------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| buffer_analysis        | 计算几何周围指定距离的范围面             | 4326→3857→`buffer(米)`→回 4326（[buffer.py](spatial/src/services/buffer.py)）                                                                             |
| distance_analysis      | 两点/两几何欧氏距离（非路网）            | 3857 下`.distance()`，保留 2 位小数（[distance.py:35-41](spatial/src/services/distance.py#L35-L41)）                                                      |
| spatial_query          | 按 bbox/类别查要素                       | 表名白名单 +`%s` 参数化 + `&& ST_MakeEnvelope` + `ST_AsGeoJSON`（[query.py](spatial/src/services/query.py)）                                              |
| route_analysis         | 真实路网最短路                           | OSMnx 实时下载路网 → 最近节点吸附 →`nx.shortest_path(weight="length")`（Dijkstra）→ 拼边几何（[route.py:49-102](spatial/src/services/route.py#L49-L102)） |
| overlay_analysis       | 两组面要素的交/并/差                     | GeoPandas`overlay(how=)`（[overlay.py](spatial/src/services/overlay.py)）                                                                                 |
| density_analysis       | 点密度热区（核密度 KDE）                 | SciPy`gaussian_kde` + 网格评估 + 0-100 归一化（[density.py:54-69](spatial/src/services/density.py#L54-L69)）                                              |
| suitability_analysis   | 多因子加权叠加选址评分                   | 权重归一化 → 网格 → 与各图层求交按面积占比加权（[suitability.py:36-101](spatial/src/services/suitability.py#L36-L101)）                                   |
| cost_distance_analysis | 考虑障碍/摩擦的累计代价面 + 最小代价路径 | **手写 Dijkstra**（见 4.5）                                                                                                                               |
| voronoi_analysis       | 生成点的服务范围划分（泰森多边形）       | Shapely`voronoi_diagram` + 边界裁剪 + 生成点回挂（[voronoi.py:50-92](spatial/src/services/voronoi.py#L50-L92)）                                           |
| mcda_analysis          | 多方案多准则排序决策                     | WSM/WPM/TOPSIS 三法（见 4.6）                                                                                                                             |
| temporal_analysis      | POI 时空统计雏形                         | COUNT/GROUP BY + ST_X/ST_Y 热点采样；真实历史数据需 Ohsome API（[temporal.py:91-97](spatial/src/services/temporal.py#L91-L97)）                           |

## 4.5 ⭐ 手写 Dijkstra 成本距离（[cost_distance.py:137-173](spatial/src/services/cost_distance.py#L137-L173)，最硬核考点）

对应 ArcGIS 的 Cost Distance 工具：从源点出发，代价（如坡度、障碍）越高的区域，经过的成本越高。

- **栅格化**：把研究区切成网格，每格与障碍要素求交取最大代价（[cost_distance.py:116-130](spatial/src/services/cost_distance.py#L116-L130)）
- **Dijkstra 传播**：优先队列（heapq）+ `dist` 数组初始 `np.inf`；**8 邻域，斜向步长 √2（1.41421356237）**（:148-157）
- **边代价公式**（:168）：`0.5 × (两端摩擦之和) × 步长 × 平均像元尺寸`——两端平均摩擦是经典处理
- **最小代价路径回溯**：目标点定位到像元 → 沿 `parent` 数组回溯（带 `seen` 防环）→ 输出 LineString（:212-246）
- 结果带 `cost_distance`（累计代价）/ `friction`（摩擦值）/ `normalized`（0-100 归一化）

💬 **面试怎么问**："为什么不用 NetworkX 现成的最短路？" → 栅格传播场景需要自己控制邻域结构与代价模型，且无显式图；能逐行讲 Dijkstra 的松弛 + 堆优化 + parent 回溯，算法功底直接拉满。

## 4.6 TOPSIS 多准则决策（[mcda.py:101-120](spatial/src/services/mcda.py#L101-L120)）

"多个候选方案 × 多个评价指标（有的越大越好 benefit，有的越小越好 cost）→ 排序"。TOPSIS 步骤：向量归一化 → 按权重加权 → 找理想最优解/最劣解 → 算每个方案到两者的距离 → 贴近度 = d*最劣/(d*最优+d\_最劣)（越大越好）。WSM（加权求和）与 WPM（加权乘积）是更简单的对照方法（:91-98）。能背出贴近度公式并解释 benefit/cost 方向处理，就是满分答案。

## 4.7 数值计算：本项目用到的 NumPy/SciPy

`gaussian_kde`（核密度）、`np.linspace/meshgrid`（网格生成）、`np.arange`（[suitability.py:53](spatial/src/services/suitability.py#L53)）、`np.clip/isfinite/full`（[cost_distance.py](spatial/src/services/cost_distance.py)）、`np.argsort`（排序取 rank）。前端岗了解到"网格 + 数组运算"层面即可，不必深入。

## 4.8 空间层面试自测（5 题）

1. 经纬度和米的换算问题出在哪？3857 有什么失真？
2. `buffer_analysis` 从请求进来到 FeatureCollection 出去，经过哪些步骤？
3. Dijkstra 成本距离的边代价公式是什么？为什么斜向要乘 √2？
4. `spatial_query` 的两层注入防护分别是什么？
5. TOPSIS 的贴近度公式是什么？benefit 和 cost 指标怎么区分处理？

---

# 第 5 章 数据库层（L2）

## 5.1 SQL 基础（本项目用到什么学什么）

- DDL：`CREATE TABLE`、类型、`PRIMARY KEY`、`SERIAL` 自增（[01-init-spatial.sql:13-38](database/init/01-init-spatial.sql#L13-L38)）
- DML：批量 `INSERT`（02 号文件 5396 条 POI 就是采集脚本生成的 INSERT 大文件）
- 查询/聚合：`SELECT ... WHERE ... LIMIT`（[query.py:83-89](spatial/src/services/query.py#L83-L89)）、`COUNT(*) ... GROUP BY category`（[temporal.py:54-62](spatial/src/services/temporal.py#L54-L62)）
- **参数化查询**：值全部走 `%s` 占位符（[query.py:91](spatial/src/services/query.py#L91)）——为什么：SQL 注入。表名/列名不能参数化，所以用**白名单**（[query.py:32-35](spatial/src/services/query.py#L32-L35)）——"表名白名单 + 值参数化"是安全面试的标准答案
- JOIN：项目暂未用（空间叠加用 GeoPandas 替代）——被问"为什么"就答"当前查询模式是单表 + bbox 过滤，跨表叠加放 Python 层做更灵活"

## 5.2 ⭐ PostGIS 核心概念

| 概念                | 项目落点                                                                                                                                                             | 讲法                                                                                                        |
| ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| 扩展机制            | `CREATE EXTENSION postgis`（[01:7-8](database/init/01-init-spatial.sql#L7-L8)）                                                                                      | PostGIS 是 PostgreSQL 的空间扩展                                                                            |
| geometry 类型族     | POINT/LINESTRING/POLYGON 对应 poi/roads/buildings 三张表（[01:19-37](database/init/01-init-spatial.sql#L19-L37)）                                                    | 矢量数据三大基本类型                                                                                        |
| SRID 两种设置       | 列级 typmod：`GEOMETRY(POLYGON, 4326)`（数据库强制拒绝其他 SRID）；值级：`ST_SetSRID(ST_MakePoint(lon,lat), 4326)`（[01:58](database/init/01-init-spatial.sql#L58)） | "贴标签"两种位置                                                                                            |
| 空间索引 GiST       | 5 张表全部`USING GIST(geom)`（[01:42-44](database/init/01-init-spatial.sql#L42-L44)）                                                                                | 空间索引基于 R-Tree 思想，用包围盒（bbox）组织                                                              |
| **`&&` 两段式过滤** | `geom && ST_MakeEnvelope(minLon,minLat,maxLon,maxLat,4326)`（[query.py:78](spatial/src/services/query.py#L78)）                                                      | `&&` 是 bbox 相交算子，**能走 GiST 索引**：先 bbox 粗筛（索引下推，快）再精确判断——空间查询性能的经典两段式 |
| ST_AsGeoJSON        | [query.py:84](spatial/src/services/query.py#L84)                                                                                                                     | 数据库内直接输出 GeoJSON，前后端免转换                                                                      |
| 属性索引            | 人口表还建了 B-tree 索引`idx_population_grid_district`（[04:14](database/init/04-populaiton-grid.sql#L14)）                                                          | 属性过滤列同样要索引——"空间索引 + 属性索引"双路过滤                                                         |

💬 **面试必问**："空间查询为什么快？" → GiST 索引 + `&&` 算子先做 bbox 粗筛（不用逐个几何算精确距离），再精确计算；对应"为什么 ST_DWithin 在数据库里算比在 Python 里算快"——数据不下库、索引下推。

## 5.3 数据采集：OSM → SQL 全链路（[fetch_osm.py](database/scripts/fetch_osm.py)）

- **OSM 数据模型**：node（点→POI）/ way（线→道路、面→建筑）/ relation（复合）三种元素 + tag 键值对
- **采集**：OSMnx（底层走 Overpass API）按 bbox 拉朝阳区数据，`features_from_bbox(tags=...)` 按 amenity/shop/highway/building 等 tag 过滤（:51-57）
- **清洗**：只保留 Point 几何、MultiPolygon 拍平取第一个子面、`maxspeed` 非数字回退 50、无名字用占位名、单引号转义防 SQL 报错（:68-174）
- **入库**：生成 `INSERT INTO poi ... ST_SetSRID(ST_MakePoint(lon,lat), 4326)` 的 SQL 文件（:84-87），挂进 `database/init/` 由 Docker 首启自动执行
- 可主动讲的槽点：脚本 main 函数有未初始化变量的分支（`output_path`/`totals`），"读代码发现采集脚本两个分支会 NameError"——又一条批判性素材

## 5.4 数据库面试自测（4 题）

1. 为什么值能参数化、表名不能参数化？本项目怎么防注入？
2. GiST 索引和普通 B-tree 索引的区别？
3. `&&` 操作符在查询里起什么作用？
4. 5 张表分别存什么、几何类型分别是什么？

---

# 第 6 章 工程化与部署（L2）

## 6.1 Docker Compose（[docker-compose.yml](docker/docker-compose.yml)）

- 4 个服务：postgis / spatial / agent / frontend + 1 个数据卷 + 1 个网络（`geoai-net`）
- **服务名即主机名**：spatial 服务连库写 `postgresql://geoai:xxx@postgis:5432/geoai`，agent 调 spatial 写 `http://spatial:8002`——容器网络内 DNS 解析
- **数据库零配置初始化**：`../database/init` 挂载到 `/docker-entrypoint-initdb.d`（:18），**只在数据卷为空时首启执行**，按文件名编号顺序跑（01→06）；已初始化过要重灌需 `docker compose down -v`
- **healthcheck 驱动启动顺序**：postgis 用 `pg_isready` 探活，spatial 用 `depends_on: condition: service_healthy` 等数据库就绪（:19-25）
- 前端 Token 用 **build args** 在构建期注入（`VITE_MAPBOX_TOKEN`）——构建期 vs 运行期环境变量的区别

## 6.2 环境变量与密钥

- 三处 .env：`docker/.env`（全部服务）、`agent/.env`（LLM 三项 + SPATIAL*SERVICE_URL）、`frontend/.env.local`（两个 VITE* Token）——按服务隔离，密钥不入库（.gitignore）
- OpenAI 兼容配置三项：`OPENAI_API_KEY` / `OPENAI_BASE_URL` / `LLM_MODEL`

## 6.3 CORS 与代理（结合 1.8 讲）

浏览器同源策略 → 跨域需服务器声明或代理规避。开发：Vite proxy；生产：Nginx 反代。后端 `allow_origins=["*"]` + `allow_credentials=True` 是开发态写法（[agent/src/main.py:51-57](agent/src/main.py#L51-L57)），生产收紧域名白名单——**主动说出这个风险就是加分**。

## 6.4 工程化面试自测（3 题）

1. `docker compose up -d` 第一次启动时数据库怎么自动建表？
2. agent 服务怎么知道 spatial 服务在哪？（容器网络 + 环境变量）
3. 生产环境怎么解决跨域？

---

# 第 7 章 面试专项

## 7.1 ⭐ 面试流程推演与话术框架

**标准流程**：自我介绍 → 项目介绍 → 逐层追问 → 开放题（优化/架构/取舍）→ 反问。

**3 分钟项目介绍模板**（背熟，可拆成 1 分钟版）：

1. **定位（30s）**："GeoAI 是城市空间智能分析平台：传统 GIS 分析门槛高，需要专业人员手动配置缓冲、叠加、权重等模型；这个项目在 GIS 引擎和用户之间加了一个 LLM Agent 决策层，让用户用自然语言完成空间分析。"
2. **架构与流程（90s）**："分四层：React+Mapbox+Cesium 前端、LangGraph ReAct Agent、FastAPI+Shapely/GeoPandas 空间服务、PostGIS 数据库。用户问'哪里适合开咖啡店'，前端把问题+地图框选上下文发到 Agent，意图解析成结构化任务，Agent 按 ReAct 循环规划并调用 buffer/density/suitability 等 11 个工具，空间服务完成 GIS 计算返回 GeoJSON，前端 2D 按分数插值着色、3D 挤出成柱，同时 RAG 检索规划规范知识生成分析报告——全程 SSE 流式，用户实时看到'思考→调工具→出结果'的心流。"
3. **亮点引导（60s）**："我最有心得的是两个：① 前端手写 SSE 流式解析 + Agent 的 stream_mode='updates' 边执行边推送；② 工具注册中心一份 Pydantic schema 同时导出给 LLM 和执行器。另外我在读代码时发现并定位了几个真实 bug，比如核密度的单位混用，您想听哪个？"——**最后一句把提问权引导到自己擅长的方向**。

**关于 AI Coding 的话术**（诚实且有力量）："这个项目是和 AI 协作开发的。我的角色是需求拆解、方案评审、代码阅读、调试和修改。所以我的强项是能讲清每一层的实现原理，并且对代码有自己的判断——比如我发现了 X、Y、Z 三个问题并修复/提出了方案。"

## 7.2 ⭐ 高频问题库（按层分组，每题给答题要点）

### 项目总览（必答，5 题）

1. **介绍一下你的项目** → 用 7.1 模板；重点讲"LLM 负责理解和规划，GIS 工具负责计算和验证"的职责分离
2. **这个项目解决什么痛点？** → 传统 GIS 门槛：专业人员手动配置模型；答案模板："把 GIS 能力从工具化变成服务化"
3. **你在这个项目里做了什么？** → 诚实讲协作方式 + 列出自己实际改过的文件（提前从 git 准备）+ 每个改动一句话理由
4. **项目里最难的模块是什么？** → 推荐答：Agent 流式心流（跨前后端的协议设计）或成本距离手写 Dijkstra（算法+GIS）
5. **数据从哪来？规模多大？** → OSM 朝阳区 5396 条 POI、道路建筑、简化行政区、示意人口格网；真实人口数据规划用 WorldPop（如实）

### 前端（重点准备，10 题）

6. **SSE 流式怎么实现的？为什么不用 EventSource？** → 1.3 节全套（POST 需求、ReadableStream、半包缓冲、stream:true、AbortError）
7. **SSE 和 WebSocket 怎么选？** → 单向推送 vs 全双工；本项目单向；WebSocket 适合聊天室
8. **Zustand 和 Redux 区别？为什么选它？** → 轻量、selector 精确订阅、心智简单；项目规模不需要 Redux 的样板
9. **Mapbox 的 source/layer 是什么关系？** → 数据 vs 样式；一 source 多 layer
10. **地图结果怎么按 score 上色的？** → expression：`interpolate linear get(score)` 分段插值
11. **2D/3D 双引擎怎么设计的？** → 共享 store 数据源、条件渲染、两套渲染策略（颜色 vs 挤出高度）
12. **地图框选如何传给后端的？** → mousedown/mouseup → 手写闭合 GeoJSON → store → context 随 query 发出
13. **React StrictMode 对地图初始化有什么影响？** → 双执行 mount；初始化函数要幂等/cleanup 完善
14. **大列表/频繁流式更新的性能怎么做？**（开放题）→ selector 订阅 + 不可变更新 + 步骤数组 append；优化方向：虚拟列表、节流
15. **前端有什么可以改进的地方？** → 图层面板显隐未落到 setLayoutProperty、MapView 死代码/冗余判断、HistoryPanel compare 半成品——选一个讲"现象→定位→修法"

### Agent/LLM（8 题）

16. **Function Calling 机制是什么？** → 3.2 节四步
17. **ReAct 是什么？你们怎么用的？** → Reasoning+Acting 循环；create_react_agent；退出条件=无 tool_call
18. **LangGraph 是什么？和 LangChain 什么关系？** → LangGraph 是图状态机编排框架（LangChain 生态）；本项目用预构建 ReAct 图；可展开手写 StateGraph 的 agent/tools 双节点条件边
19. **RAG 是什么？为什么需要？你们怎么做的？** → 3.5 节；知识时效/私有知识/幻觉；55 条知识→ONNX embedding→Chroma→检索拼 prompt→引用来源
20. **embedding 是什么？** → 文本转向量，语义相近距离近
21. **温度参数是什么？项目里怎么设的？** → 随机性；0/0/0.3 三处及理由
22. **怎么保证 LLM 输出格式正确？** → 路线②：JSON 模板 prompt + 三级容错提取；对比路线①原生 JSON mode；为什么不能用（DeepSeek 不支持 response_format）
23. **Agent 的上下文/记忆怎么管理？** → 无 checkpointer 无状态设计 + 自建 SQLite 历史；框架记忆 vs 业务记忆的取舍

### GIS/空间服务（8 题）

24. **为什么 buffer 要先转 3857？** → 度≠米；3857 失真；always_xy
25. **SQL 注入怎么防？** → 白名单 + 参数化；为什么表名不能参数化
26. **GiST 索引原理？** → R-Tree bbox 组织；`&&` 两段式过滤
27. **成本距离算法怎么实现的？** → 4.5 节：栅格化→Dijkstra 8 邻域√2→边代价公式→parent 回溯
28. **TOPSIS 是什么？** → 4.6 节：理想解贴近度
29. **核密度分析原理？** → KDE：每个点贡献一个核函数，叠加出密度面；带宽控制平滑程度
30. **GeoJSON 是什么？** → RFC 7946 地理数据交换格式；FeatureCollection；首尾闭合
31. **空间分析结果怎么回到地图？** → GeoJSON 全链路：ST_AsGeoJSON / **geo_interface** → HTTP → addSource / GeoJsonDataSource

### 数据库/工程化（5 题）

32. **数据库怎么初始化/迁移的？** → docker-entrypoint-initdb.d 首启执行；编号顺序；重灌 down -v
33. **服务间怎么通信？** → 容器网络服务名 + 环境变量 URL
34. **跨域怎么解决的？** → Vite proxy / Nginx；CORS 开发态配置的风险
35. **为什么 agent 和 spatial 拆两个服务？** → 关注点分离：智能决策 vs GIS 计算；独立扩缩容/独立替换模型；LLM 调用是长耗时不可控环节，与计算服务隔离互不影响
36. **如果用户量上来，哪里先成为瓶颈？** → Python 层双层循环无空间索引（O(n·m)）、单连接 psycopg2、报告非流式、无缓存——逐个给改进方案

### 开放题（4 题，展示格局）

37. **如果让你优化这个项目，做哪三件事？** → ① 空间层加 STRtree/sindex 空间索引 + 连接池（asyncpg/SQLAlchemy）② token 级流式（stream_mode="messages"）+ checkpointer 多轮记忆 ③ 修真实 bug（density 单位、route bbox）+ 补测试（目前几乎零测试）
38. **LLM 不可靠怎么办？** → 温度控制、结构化输出三级容错、工具错误带 hint 驱动自纠错、能力边界判 unsupported、结果让用户确认——本项目的工程防御清单
39. **多 Agent 协作怎么做？**（设计文档有规划）→ 拆空间分析/数据/报告/可视化 Agent，各自专注；难点在任务分发与状态共享
40. **这个项目最大的挑战是什么？**（准备自己的故事）→ 例：AI 生成代码的 bug 定位（讲 density 单位 bug 的定位过程：读代码发现坐标已转 3857 却除以 111320"转度"，自相矛盾）

## 7.3 ⭐ 主动出击：代码盘点素材库（12 个可讲的 bug/演进点）

面试官问"项目有什么问题"时，按"现象 → 定位 → 原因 → 修法"讲 2-3 个最熟的：

| #   | 问题                 | 位置                                                                          | 一句话讲法                                                             |
| --- | -------------------- | ----------------------------------------------------------------------------- | ---------------------------------------------------------------------- |
| 1   | KDE 单位混用         | [density.py:54](spatial/src/services/density.py#L54)                          | 坐标已转 3857 米制，带宽却除以 111320"转度"，带宽被缩小约 11 万倍      |
| 2   | route bbox 南北翻转  | [route.py:43](spatial/src/services/route.py#L43)                              | `max_lat = min(...)` 应为 max，起点纬度高于终点时 bbox 退化            |
| 3   | 距离被当相似度       | [vector_store.py:71](agent/src/knowledge/vector_store.py#L71)                 | Chroma 返回的是距离（越小越相似），命名为 score 语义反了；且无阈值过滤 |
| 4   | "连接池"实为单连接   | [database.py:14](spatial/src/database.py#L14)                                 | 注释自称简单连接池，实际一个全局连接，多 worker 下有隐患               |
| 5   | 采集脚本会 NameError | [fetch_osm.py:223-234](database/scripts/fetch_osm.py#L223-L234)               | `output_path`/`totals` 未初始化的分支                                  |
| 6   | 图层面板显隐未落地   | LayerPanel → store                                                            | 只改 store 未调`map.setLayoutProperty`，勾选不生效                     |
| 7   | 死代码/冗余          | [MapView.tsx:115-118](frontend/src/components/map/MapView.tsx#L115-L118)、:80 | cases 数组未使用；loaded 判断冗余且有漏渲染时序风险                    |
| 8   | compare 视图半成品   | [HistoryPanel.tsx:145](frontend/src/components/chat/HistoryPanel.tsx#L145)    | setView("compare") 无对应 UI                                           |
| 9   | CORS 全放开          | [agent/src/main.py:51-57](agent/src/main.py#L51-L57)                          | `*` + credentials 是开发态，生产必须收紧                               |
| 10  | 报告非 token 级流式  | [routes.py:194-200](agent/src/api/routes.py#L194-L200)                        | 阶段级流式，报告整块到；演进方向 stream_mode="messages"                |
| 11  | 3857 面积失真        | [buffer.py:49](spatial/src/services/buffer.py#L49)                            | area_sqm 在 Web Mercator 下算，高纬偏大                                |
| 12  | 几乎零测试           | agent/tests、spatial/tests                                                    | 只有空`__init__.py`——改进方向必答                                      |

## 7.4 别踩的坑（红线清单）

- ❌ 别说用了 PySAL（依赖里没有，密度是 SciPy）
- ❌ 别说实现了 MCP 协议（是"MCP 风格"的自研注册中心）
- ❌ 别说做了遥感 AI 识别（只有设计文档）
- ❌ 别把 3D Tiles 全城模型说成已实现（当前是 entities 挤出）
- ❌ 别说时空分析有历史数据（当前是 POI 当前态统计）
- ✅ 正确口径：把"规划 vs 实现"的边界主动说清（见 0.4 表），这本身就是严谨加分

---

# 第 8 章 4 周冲刺计划（每天 5 小时）

**每日时间块模板**：2h 学知识点 → 2h 读对应项目代码 → 1h 动手练习/复盘笔记。第 4 周后半段切换为全天面试模拟。
**每日产出物**：① 一段 200 字总结（存笔记）② 练习代码 ③ 睡前 10 分钟对 0.3 主线自述。

### Week 1：前端核心 + 项目全景（对应第 0、1 章）

| 天  | 主题                                           | 动手练习（产出物）                                                                                                   |
| --- | ---------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| D1  | 项目跑起来（README 本地启动四步）+ 第 0 章全景 | 亲手发一次 query，把 0.3 的 24 步对着实际请求走一遍；笔记：完整链路图                                                |
| D2  | TypeScript（1.1 清单逐项）                     | 给[types/index.ts](frontend/src/types/index.ts) 每个类型写注释；写一个含泛型/字面量联合/Record 的小 demo             |
| D3  | Zustand（1.2）                                 | 通读[store/index.ts](frontend/src/store/index.ts) 并画状态图；用 Zustand 重写一个计数器+购物车 demo（selector 订阅） |
| D4  | SSE 前端解析（1.3）⭐                          | 逐行抄写[agent.ts:21-72](frontend/src/services/agent.ts#L21-L72) 并加注释；用 Node 写一个最小 SSE 服务器对接它       |
| D5  | 数据流全景（1.1-1.3 串联）                     | 画 submitQuery→queryStream→回调→UI 的时序图；回答 1.9 五题                                                           |
| D6  | React 细节 + 工程化（1.7-1.8）                 | 精读 AppLayout 拖拽实现（ref 模式）；写一份 vite 代理原理解释笔记                                                    |
| D7  | 周复盘                                         | 不碰代码，闭卷 5 分钟讲 0.3 主线 + 1.9 五题；错漏处回看                                                              |

### Week 2：地图与 GeoJSON（对应 1.4-1.6 + 4.1 坐标系提前学）

| 天  | 主题                                 | 动手练习                                                                                                                                                                               |
| --- | ------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D8  | Mapbox 基础（1.4 上半）              | 独立小项目：初始化地图 + 加载一个手写 GeoJSON 数据源 + fill 图层                                                                                                                       |
| D9  | Mapbox 进阶（expression + 事件）     | 给上面的 demo 加 score 插值着色 + fitBounds + moveend 同步 bounds                                                                                                                      |
| D10 | Cesium（1.5）                        | 独立 demo：Viewer + 一个挤出柱 entity（extrudedHeight）+ 相机 flyTo                                                                                                                    |
| D11 | GeoJSON 规范 + 坐标系（1.6 + 4.1）⭐ | 手写三种几何的 GeoJSON（含闭合 Polygon）；写一篇"度≠米"笔记（4326/3857/失真/always_xy）                                                                                                |
| D12 | 双引擎映射精读                       | 对照读[MapView.tsx:98-170](frontend/src/components/map/MapView.tsx#L98-L170) 与 [CesiumView.tsx:19-58](frontend/src/components/map/CesiumView.tsx#L19-L58)，画"同一数据两套渲染"对比表 |
| D13 | 地图交互闭环                         | 精读 DrawTool → store → context 链路；给框选加一个"显示框选面积"的小改动（你的第一个真实改动！）                                                                                       |
| D14 | 周复盘                               | 闭卷讲：source/layer、expression、挤出柱、框选链路；答 1.9 第 3-4 题 + 坐标系三连问                                                                                                    |

### Week 3：Python + Agent/LLM（对应第 2、3 章）

| 天  | 主题                             | 动手练习                                                                                                                                                                |
| --- | -------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D15 | Python 语法点（2.1）             | 把第 2.4 节练习 1 做掉（FastAPI + Pydantic 接口）                                                                                                                       |
| D16 | FastAPI/Pydantic（2.2-2.3）      | 第 2.4 节练习 2（SSE 接口 + 前端对接）；通读[buffer.py](spatial/src/services/buffer.py) 写逐行注释                                                                      |
| D17 | LLM 基础 + 兼容 API（3.1-3.2）⭐ | 用 Python/curl 直接调一次 OpenAI 兼容 API（配 .env 的 base_url）；笔记：messages/温度/Function Calling 四步                                                             |
| D18 | ReAct + LangGraph（3.3）⭐       | 通读[planner.py](agent/src/planning/planner.py) 全文，画 agent⇄tools 循环图 + stream_mode 对比表                                                                        |
| D19 | 工具注册（3.4）                  | 通读[registry.py](agent/src/tools/registry.py) + distance 降级链路（[spatial_tools.py:151-183](agent/src/tools/spatial_tools.py#L151-L183)）；笔记：错误如何变成 prompt |
| D20 | RAG（3.5）⭐                     | 通读[vector_store.py](agent/src/knowledge/vector_store.py) + [interpreter.py](agent/src/reasoning/interpreter.py)；跑一个 10 条文本的 Chroma 检索 demo                  |
| D21 | 周复盘                           | 闭卷讲 Function Calling 四步、ReAct 循环、RAG 链路；答 3.8 五题                                                                                                         |

### Week 4：GIS 服务 + 数据库 + 面试冲刺（对应第 4-7 章）

| 天  | 主题                               | 动手练习                                                                                                                                                         |
| --- | ---------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| D22 | 空间服务层（4.1-4.3）              | 通读 buffer/distance/query/overlay 四个服务；用 Shapely 写"buffer 前先转投影"的对比实验（4326 直接 buffer vs 3857）                                              |
| D23 | 算法深度（4.4-4.6）⭐              | 通读[cost_distance.py](spatial/src/services/cost_distance.py)（249 行逐行）+ [mcda.py](spatial/src/services/mcda.py)；手写一遍 10×10 网格的 Dijkstra（任何语言） |
| D24 | 数据库（第 5 章）                  | 用 psql 连库跑：`SELECT count(*) FROM poi`、写一条带 `&&` 的 bbox 查询看执行计划（EXPLAIN）；笔记：GiST 两段式                                                   |
| D25 | 数据采集 + 工程化（5.3 + 第 6 章） | 读[fetch_osm.py](database/scripts/fetch_osm.py) 全文；`docker compose ps` 观察四服务；笔记：docker 初始化钩子原理                                                |
| D26 | 全链路串联 + bug 库                | 把 0.3 主线讲到 10 分钟深度版（含每层原理）；从 7.3 挑 3 个 bug 各写"现象→定位→原因→修法"                                                                        |
| D27 | 模拟面试日                         | 对着 7.2 的 40 题自问自答（录音/录视频）；7.1 的 3 分钟介绍背到脱口而出                                                                                          |
| D28 | 查漏补缺                           | 复答所有答不出的题；补读薄弱层代码；最终产出：一份自己的"面试弹药笔记"（介绍模板 + 主线图 + bug 清单 + 自答题库）                                                |

---

# 附录 A 术语速查表（面试前过一遍，要求每个都能一句话解释）

| 术语                                 | 一句话解释                                             |
| ------------------------------------ | ------------------------------------------------------ |
| LLM                                  | 大语言模型，预测下一个 token 的神经网络                |
| token                                | 模型处理文本的最小单位，计费和上下文窗口的单位         |
| 上下文窗口                           | 一次请求能容纳的最大 token 数                          |
| temperature                          | 采样随机性参数，0 确定 1 发散                          |
| prompt                               | 发给模型的输入指令                                     |
| Function Calling                     | 模型输出"调哪个函数+参数"的 JSON，由程序执行后回填     |
| Tool / 工具调用                      | 同上，Agent 语境下的叫法                               |
| ReAct                                | Reasoning+Acting 交替循环的 Agent 范式                 |
| Agent                                | LLM + 工具 + 决策循环的自主任务执行者                  |
| LangChain                            | LLM 应用开发框架（消息、工具、链）                     |
| LangGraph                            | 图状态机编排框架，可编排多节点 Agent 流程              |
| StateGraph / ToolNode / checkpointer | 图结构 / 执行工具调用的内置节点 / 对话状态持久化器     |
| streaming / SSE                      | 服务器→客户端的单向长连接流式推送                      |
| RAG                                  | 检索增强生成：先检索知识库再生成，减少幻觉             |
| embedding                            | 文本转高维向量，语义相近向量相近                       |
| 向量数据库                           | 存向量并支持相似度检索的数据库（Chroma）               |
| OpenAI 兼容 API                      | 事实标准的 LLM HTTP 协议，换 base_url 即可换厂商       |
| FastAPI / uvicorn / ASGI             | Python Web 框架 / ASGI 服务器 / 异步网关接口标准       |
| CORS                                 | 跨域资源共享，浏览器同源策略的放宽机制                 |
| Pydantic                             | Python 数据校验/序列化库，定义接口 schema              |
| GeoJSON                              | RFC 7946 地理数据交换格式（FeatureCollection/Feature） |
| WKT / WKB                            | 文本/二进制几何表示格式                                |
| SRID / EPSG                          | 空间参考系标识符（如 4326=WGS84）                      |
| WGS84 / Web Mercator                 | 经纬度坐标系 / 米制平面投影（3857）                    |
| buffer / overlay                     | 缓冲区分析 / 叠置分析（交并差）                        |
| KDE 核密度                           | 用核函数估计点分布的密度面                             |
| Voronoi 泰森多边形                   | 按最近原则划分服务范围                                 |
| Dijkstra                             | 单源最短路算法（堆优化）                               |
| 成本距离                             | 考虑障碍/摩擦的累计代价传播                            |
| 加权叠加                             | 多因子按权重求和得到综合评分                           |
| TOPSIS                               | 基于理想解距离的多准则决策排序                         |
| GiST / R-Tree                        | PostgreSQL 通用索引 / 空间数据树形索引结构             |
| `&&` 算子                            | PostGIS 包围盒相交操作符，可走 GiST 索引               |
| ST_DWithin                           | PostGIS 距离判断函数（本项目未用，Phase 2 规划）       |
| ST_SetSRID / ST_Transform            | 贴坐标系标签（不改值）/ 真重投影（改值）               |
| 参数化查询                           | 值用占位符传参，防 SQL 注入                            |
| Docker Compose                       | 多容器编排工具（服务+网络+卷）                         |
| healthcheck                          | 容器健康探针，驱动启动顺序                             |
| OSM / Overpass / OSMnx               | 开源地图数据 / 其查询 API / 其 Python 下载库           |
| GeoPandas / Shapely                  | 空间数据表格 / 几何计算引擎                            |
| 3D Tiles                             | Cesium 的大规模三维数据格式（本项目未用）              |
| Zustand selector                     | 精确订阅单个状态字段，避免无关重渲染                   |
| AbortController                      | 取消 fetch/异步操作的控制器                            |
| ReadableStream                       | fetch 响应体的流式读取接口                             |
| expression（Mapbox）                 | 声明式数据驱动样式语言                                 |

# 附录 B 资源推荐（每主题 1-3 个，按周引用）

- **TypeScript**：TS 官方 Handbook（重点看 Object Types / Generics / Type Narrowing）
- **Zustand**：官方文档（很短，半天读完）
- **Mapbox GL**：官方 docs 的 example 页（亲手跑 5 个例子：source/layer/expression/fitBounds/marker）
- **Cesium**：官方 Tutorial（Viewer → Entities → Camera 三节即可）
- **Python/FastAPI**：FastAPI 官方 Tutorial（前 8 章）；Python 官方 tutorial 补语法
- **LLM/Agent**：DeepLearning.AI 短课《Function Calling and Data Extraction with LLMs》《LangGraph for Agentic Applications》《RAG》；LangGraph 官方文档概念篇
- **坐标系**：EPSG.io 查任何 SRID；搜"Web Mercator 失真"（The True Size Of 交互站）建立直观感受
- **GIS**：GeoPandas 官方 Getting Started；PostGIS 官方 Intro to PostGIS（前 5 章）
- **SQL**：SQLBolt（交互练习，2 小时过完）
- **Docker**：官方 Get Started + 本项目 compose 文件逐行读

# 附录 C 代码位置索引（面试前必读文件清单，按优先级）

**必读（逐行）**：

1. [agent.ts](frontend/src/services/agent.ts) — SSE 解析（96 行）
2. [store/index.ts](frontend/src/store/index.ts) — 状态设计（169 行）
3. [MapView.tsx](frontend/src/components/map/MapView.tsx) — Mapbox（192 行）
4. [CesiumView.tsx](frontend/src/components/map/CesiumView.tsx) — Cesium（140 行）
5. [planner.py](agent/src/planning/planner.py) — ReAct 流式核心（221 行）
6. [registry.py](agent/src/tools/registry.py) + [spatial_tools.py](agent/src/tools/spatial_tools.py) — 工具体系
7. [parser.py](agent/src/intent/parser.py) — 结构化输出（104 行）
8. [interpreter.py](agent/src/reasoning/interpreter.py) + [vector_store.py](agent/src/knowledge/vector_store.py) — RAG
9. [buffer.py](spatial/src/services/buffer.py) — 坐标系转换范本（58 行）
10. [query.py](spatial/src/services/query.py) — SQL 安全（110 行）
11. [cost_distance.py](spatial/src/services/cost_distance.py) — Dijkstra（249 行）
12. [01-init-spatial.sql](database/init/01-init-spatial.sql) — schema（70 行）

**选读**：routes.py（SSE 协议）、schemas.py（Pydantic）、overlay/density/route/voronoi/mcda/temporal（各服务）、history.py、fetch_osm.py、docker-compose.yml、vite.config.ts

---

_最后提醒：这份文档的每一句"面试怎么讲"都要**用自己的话复述一遍**才算数。四周结束后，终极检验标准是——不翻文档，对着 0.3 的主线 + 7.2 的题库，连续讲满 40 分钟不卡壳。_
