# GeoAI Spatial Agent — LLM + LangGraph
FROM python:3.12-slim

WORKDIR /app

RUN pip install uv

COPY pyproject.toml .
RUN uv sync --frozen 2>/dev/null || uv sync

COPY src/ src/

EXPOSE 8001
CMD ["uv", "run", "uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "8001"]
