# 第一章：GeoAI 城市空间智能分析平台总体设计

## 1.1 项目定位与建设背景

随着数字孪生、智慧城市、自然资源管理和低空经济等领域的发展，传统 GIS 系统正在从“空间信息展示工具”向“空间智能决策平台”演进。

传统 WebGIS 的核心能力主要集中在：

- 地图浏览；
- 图层管理；
- 空间查询；
- 人工配置空间分析模型；
- 输出统计结果。

例如，在一个商业选址场景中，GIS 专业人员需要手动完成：

1. 加载研究区域；
2. 导入道路、POI、人口、土地利用等数据；
3. 设置空间分析指标；
4. 配置缓冲区、距离分析、网络分析、叠置分析等算法；
5. 设置权重模型；
6. 执行空间计算；
7. 人工解释结果。

对于 GIS 专业人员而言，这是一套成熟流程，但对于普通决策者而言，空间分析仍然具有较高门槛。

因此，本项目提出：

> 构建一个基于大语言模型 Agent、空间数据库和 WebGIS 技术的 GeoAI 城市空间智能分析平台，使用户能够通过自然语言或地图交互直接提出空间问题，由 AI 自动理解需求、规划分析流程、调用 GIS 工具完成空间计算，并通过三维地图和智能报告反馈决策结果。

项目目标不是替代 GIS 算法，而是通过 AI 将已有 GIS 能力进行智能编排和自动化调用。

简单来说：

传统 GIS：

```
用户
 |
GIS专业人员
 |
空间分析工具
 |
分析结果
```

GeoAI 平台：

```
用户
 |
自然语言/地图交互
 |
Spatial Agent
 |
GIS分析工具链
 |
空间计算
 |
可视化结果 + 分析报告
```

---

# 1.2 核心设计理念：从 WebGIS 到 Spatial Intelligence

## 1.2.1 传统 WebGIS 的局限

传统 WebGIS 更关注：

“空间数据在哪里？”

例如：

点击一个建筑：

返回：

```
建筑名称：
XX大厦

高度：
80m

面积：
5000㎡
```

它解决的是空间信息查询问题。

但是现实业务更关注：“这个空间意味着什么？”

例如用户提出：

> “分析上海某区域是否适合建设大型商业综合体。”

真正需要回答：

- 周边人口是否足够？
- 道路交通是否便利？
- 周围商业竞争是否激烈？
- 土地性质是否允许？
- 未来发展趋势如何？
- 哪些区域风险最低？

这已经不是简单的数据查询，而是空间推理问题。

因此平台核心能力从：

```
空间可视化
```

升级为：

```
空间理解 + 空间推理 + 空间决策
```

---

# 1.3 系统总体架构设计

整个系统分为六个核心层次：

```
┌──────────────────────────────┐
│          用户交互层            │
│ React + Cesium + Mapbox       │
│ 自然语言输入 / 地图交互        │
└───────────────▲──────────────┘
                │
                │
┌───────────────┴──────────────┐
│        Spatial Agent层        │
│ LLM + MCP + Function Calling  │
│ 意图识别 / 任务规划 / 工具调用 │
└───────────────▲──────────────┘
                │
                │
┌───────────────┴──────────────┐
│        空间能力服务层          │
│ Python GIS Engine             │
│ 空间分析 / 遥感分析 / AI模型   │
└───────────────▲──────────────┘
                │
                │
┌───────────────┴──────────────┐
│        空间数据层              │
│ PostgreSQL + PostGIS          │
│ GeoServer                     │
│ Raster / Vector Data          │
└───────────────▲──────────────┘
                │
                │
┌───────────────┴──────────────┐
│        数据采集层              │
│ OSM / 遥感 / 点云 / IoT       │
└──────────────────────────────┘
```

---

# 1.4 各层技术职责

---

# 1.4.1 用户交互层

## 技术：

前端：

- React
- TypeScript
- CesiumJS
- Three.js
- Mapbox GL JS

主要负责：

## （1）二维地图交互

例如：

用户框选区域：

```
Polygon
|
经纬度坐标
|
发送给后端
```

数据格式：

GeoJSON：

```json
{
"type":"Polygon",
"coordinates":[[
[116.3,39.9],
[116.4,39.9],
[116.4,40],
[116.3,40]
]]
}
```

---

## （2）三维空间展示

Cesium 负责：

- 地球渲染；
- 地形加载；
- 建筑模型；
- 3D Tiles；
- 空间分析结果展示。

例如：

空间分析结果：

后端返回：

```json
{
"type":"FeatureCollection",
"features":[
{
"type":"Feature",
"properties":{
"score":95
},
"geometry":{
...
}
}
]
}
```

前端转换为 Cesium：

```javascript
Cesium.GeoJsonDataSource.load(data)
```

展示：

高价值区域：红色

中价值：黄色

低价值：蓝色

---

# 1.4.2 Spatial Agent 层

这是整个项目区别于传统 GIS 的核心。

## 技术：

- LLM
- Function Calling
- MCP
- Agent Framework

可选择：

- LangChain
- LangGraph
- AutoGen
- OpenAI Agents SDK

---

## 核心职责：

把用户语言转换成GIS任务。

例如：

> 找出北京朝阳区适合开咖啡店的位置。

LLM解析：

```json
{
"task":"location_selection",
"industry":"coffee",
"location":"北京市朝阳区",
"criteria":["population_density", "poi_density", "transport_accessibility"]
}
```

然后规划，需要调用：

```
Tool1:查询POI
Tool2:人口分析
Tool3:道路可达性分析
Tool4:综合评分
```

---

# 1.4.3 空间能力服务层

这是 GIS 真正执行的位置。

技术：

Python：

- GeoPandas
- Shapely
- PySAL
- Rasterio
- NetworkX
- OSMnx

通过 FastAPI 暴露接口。

例如：

Agent调用：

```
POST

/api/spatial/suitability-analysis
```

参数：

```json
{
"region":"朝阳区",
"criteria":{"population":0.4, "transport":0.3, "poi":0.3}
}
```

Python执行空间分析。

返回：

```json
{
"score":85,
"geometry":{

}
}
```

---

# 1.4.4 空间数据层

## PostgreSQL + PostGIS

负责：矢量空间数据存储。

例如：

建筑：

```
building
--------------
id
height
usage
geom
```

其中：

geom：PostGIS Geometry类型。

支持：点POINT、线LINESTRING、面POLYGON

---

空间索引采用：GiST Index

例如：

```sql
CREATE INDEX idx_building_geom
ON building
USING GIST(geom);
```

作用是快速查询：“某区域附近建筑”。

---

空间查询：

例如：

查询500米范围内建筑：

```sql
SELECT * FROM building
WHERE ST_DWithin( geom, point, 500);
```

对应空间关系：

```
空间对象A
距离
空间对象B
≤500米
```

---

# 1.4.5 数据采集层

数据来源：

## （1）OpenStreetMap

提供：

- 道路
- 建筑
- POI

获取：OSM API

处理：OSMnx：

```python
ox.graph_from_place()
```

生成道路网络。

---

## （2）遥感数据

来源：

- Sentinel-2
- Landsat

格式：GeoTIFF

处理Rasterio：

```python
rasterio.open()
```

读取像元值用于土地覆盖分类。

```
Band1
Band2
Band3
...
```

---

## （3）点云数据

来源：LiDAR。

处理：PDAL、Open3D

用途：生成三维建筑、地形模型

---

# 1.5 六大核心能力规划

整个系统最终形成六个核心能力。

---

# 能力一：自然语言驱动空间分析 Agent

目标：让用户不用学习 GIS 工具，通过语言完成空间分析。

例如：

> “帮我分析深圳南山区未来商业发展潜力。”

Agent自动：

- 识别区域；
- 选择指标；
- 调用空间分析工具；
- 生成报告。

核心技术：LLM + MCP + GIS Tool Chain。

---

# 能力二：空间分析计算引擎

目标：建立可被 Agent 调用的 GIS 分析能力。

包含：

- 距离分析；
- 缓冲区分析；
- 网络分析；
- 成本距离；
- 适宜性评价。

技术：GeoPandas、Shapely、PostGIS、NetworkX、PySAL

---

# 能力三：时空变化分析

目标：理解城市如何变化。

例如：2018-2025，建筑增长+35%、绿地下降-12%

技术：PostGIS Temporal、Raster Analysis、Change Detection

---

# 能力四：遥感 AI 识别

目标：让系统理解地表。

例如：

自动识别：

- 建筑
- 道路
- 水体
- 植被

技术：PyTorch、YOLO、SAM、SegFormer、Rasterio

---

# 能力五：三维城市数字孪生

目标：构建真实空间环境。

技术：Cesium、3D Tiles、WebGL、Three.js

展示：

- 建筑
- 点云
- 地形
- 分析结果

---

# 能力六：智能空间报告生成

目标：将空间计算结果转化为决策语言。

输入空间指标：

```
人口密度
道路距离
商业数量
土地类型
```

输出自然语言报告。

技术：LLM、RAG、Structured Output

---

# 1.6 项目最终形态

最终用户体验：

```
用户：寻找适合建设新能源汽车充电站的位置
↓
Spatial Agent
↓
自动分析：交通网络、车辆流量、人口分布、土地条件
↓
GIS Engine执行
↓
Cesium展示候选区域
↓
AI生成报告：推荐区域A
原因：距离主干道300m、周边居民覆盖高、土地限制低
风险：施工成本较高
建议：优先建设中型站点
```

---

# 1.7 项目技术价值总结

该项目不是：“React + 地图展示”。

而是：

```
前端工程 + 三维可视化 + 空间数据库 + GIS算法 + AI Agent + 遥感智能识别 + 时空分析
```

形成完整闭环：

```
空间数据获取
↓
空间数据理解
↓
空间计算
↓
AI推理
↓
空间决策
↓
可视化表达
```

这也是未来“空间智能（Spatial Intelligence）”的发展方向。

---

